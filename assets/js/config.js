/* ============================================================
   CONFIG — your Supabase project credentials.
   Find these in: Supabase dashboard → Project Settings → API Keys.
   Supabase is retiring the legacy "anon" key in favor of the new
   "publishable" key (sb_publishable_...) — same purpose (safe to
   ship in client code, access is controlled by the Row Level
   Security policies in supabase_schema.sql), just a new format.
   supabase-js accepts either one as the second createClient() arg.
   ============================================================ */

const CONFIG = {
  SUPABASE_URL: "https://xqwyfndxflsameopvylt.supabase.co",
  SUPABASE_ANON_KEY: "sb_publishable_3WpBlkjbIQJQLmHriPjTcw_Y4fj5Nv9",
};
